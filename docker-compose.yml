services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: pasword@123
      MYSQL_DATABASE: issue-tracker
      MYSQL_USER: root
      MYSQL_PASSWORD: user@123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
